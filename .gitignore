# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variables d'environnement
.env                # Fichier générique — NE PAS COMMITER (peut contenir des secrets)
.env.local          # Dev local — NE PAS COMMITER (remplace .env en local)
# .env.production est commité intentionnellement : il contient uniquement l'URL publique du backend Render
backend/.env        # Secrets backend — NE PAS COMMITER

# Base de données SQLite locale (données sensibles)
backend/*.db
database/*.db
